import { Injectable } from '@angular/core';

/**
 * @class JustServiceService
 * @classdesc A class that represents the JustServiceService.
 *
 * @constructor
 * Creates an instance of JustServiceService.
 */
@Injectable({
  providedIn: 'root'
})
export class JustServiceService {

  constructor() { }

  /**
   * itDoesNothing method does nothing.
   * This method is just a placeholder and does not perform any operations.
   *
   * @returns {undefined} This method does not return anything.
   */
  itDoesNothing() {
    // and I keep it
  }
}
